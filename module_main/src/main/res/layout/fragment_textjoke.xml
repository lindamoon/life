<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:binding="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="textjoke"
            type="top.lixb.base.module_main.viewmodel.TextJokeViewModel" />

        <import type="me.tatarka.bindingcollectionadapter2.LayoutManagers" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.scwang.smartrefresh.layout.SmartRefreshLayout
            android:id="@+id/srefresh"
            app:srlEnableRefresh="false"
            binding:onloadmoreListener="@{textjoke.loadmoreListener}"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <android.support.v7.widget.RecyclerView
                android:id="@+id/list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:adapter="@{textjoke.adapter}"
                app:itemBinding="@{textjoke.singleItem}"
                app:items="@{textjoke.items}"
                app:layoutManager="@{LayoutManagers.linear()}"
                app:viewHolder="@{textjoke.viewHolder}" />
        </com.scwang.smartrefresh.layout.SmartRefreshLayout>
    </LinearLayout>
</layout>
